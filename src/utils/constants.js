const ROLES = {
    ADMIN: 'admin',
    CLIENT: 'client'
}

const ITEM_STATUS = {
    APPROVED: 'approved',
    REJECTED: 'rejected',
    WAITING: 'waiting'
}

const ITEM_TYPES = {
    LOST: 'lost',
    FOUND: 'found'
}

module.exports = {
    ROLES,
    ITEM_STATUS,
    ITEM_TYPES
}